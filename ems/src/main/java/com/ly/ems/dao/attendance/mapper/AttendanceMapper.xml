<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ly.ems.dao.attendance.AttendanceMapper" >

    <resultMap id="BaseAttendanceMap" type="com.ly.ems.model.attendance.AttendanceDTO">
        <id column="id" property="id" jdbcType="INTEGER" />
    </resultMap>

    <select id="getAttendancesByConditions" resultMap="BaseAttendanceMap">
        SELECT a.* FROM ${attendanceTableName} a WHERE 1=1 AND status = 1
        <if test="conditions.employeeName != null and conditions.employeeName != ''"> AND employee_name like CONCAT('%', #{conditions.employeeName, jdbcType=VARCHAR}, '%') </if>
        <if test="conditions.groupId != null and conditions.groupId != ''"> AND group_id = #{conditions.groupId, jdbcType=VARCHAR} </if>
        <if test="conditions.jobId != null and conditions.jobId != ''"> AND job_id = #{conditions.jobId, jdbcType=VARCHAR} </if>
    </select>

    <select id="isExistAttendanceTable" parameterType="String" resultType="Integer">
        select count(1) from INFORMATION_SCHEMA.tables where table_name = '${attendanceTableName}'
    </select>

    <update id="createAttendanceTable" parameterType="String">
        create table ${attendanceTableName} (
            id int not null generated by default as identity,
            author_id int not null,
            title varchar(255),
            primary key (id)
        )
    </update>



</mapper>