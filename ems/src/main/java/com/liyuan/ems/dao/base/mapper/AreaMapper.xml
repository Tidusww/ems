<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.liyuan.ems.dao.base.AreaMapper" >

    <resultMap id="BaseAreaMap" type="com.liyuan.ems.model.base.area.Area">
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="area_name" property="areaName" jdbcType="VARCHAR" />
        <result column="status" property="status" jdbcType="INTEGER" />
    </resultMap>

    <select id="getAreaById" resultMap="BaseAreaMap">
        SELECT a.* FROM ly_area a WHERE a.id = #{id, jdbcType=INTEGER}
    </select>

    <select id="getAreasByConditions" parameterType="com.liyuan.ems.model.base.area.AreaConditions" resultMap="BaseAreaMap">
        SELECT a.* FROM ly_area a WHERE 1=1 AND status = 1
        <if test="areaName != null and areaName != ''"> AND j.area_name like CONCAT('%', #{areaName, jdbcType=VARCHAR}, '%') </if>
    </select>
</mapper>